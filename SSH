SSH into server:

ssh root@134.209.151.102
password: love

cd AurovilleConnect

Always do after updating a file:
git pull origin main
docker-compose down
docker-compose build
docker-compose up -d

# View logs if needed:
docker-compose logs -f 